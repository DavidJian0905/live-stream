<%= link_to "Create New Video", new_video_path %></br>
  <% @videos.each do |video| %>
<div class="card">
    <h2><%= video.title %></h2>
    <%= video_tag(video.video_url.to_s, :controls => true) %>

  <% end %>
</div>
